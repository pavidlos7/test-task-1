import styled from 'styled-components';
import constants from '../../../constants';

export default styled.div`
  height: ${constants.heights.header}px;
  padding: 10px 0;
  background: ${constants.colors.background2};
`;
