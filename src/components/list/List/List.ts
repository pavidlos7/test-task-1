import styled from 'styled-components';
import constants from '../../../constants';

export default styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: ${constants.card.indent}px;
`;
