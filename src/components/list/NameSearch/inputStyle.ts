import { css } from 'styled-components';
import constants from '../../../constants';

export default css`
  padding: 2px 5px;
  border: 1px solid ${constants.colors.border1};
  background: ${constants.colors.background4};
  border-radius: 5px;
`;
